# S4 deep plan red-team adjudication

Date: 2026-09-12. Plan review only. Four independent reviewers returned 14 findings across security, failure modes, assumptions, and scope/contracts. Their original reports are snapshots of the draft they read; this document records the controller's evidence-based disposition against the refined plan. No reviewer performed implementation acceptance.

## Dispositions

| Lens / severity | Finding | Disposition and final owning surface |
|---|---|---|
| Security / High | Broad native auth routes expose unnecessary account/session surface | Accepted. Contracts and Phase3 allow only exact sign-in/email and sign-out POSTs; token-free sign-in projection preserves cookies/status/Retry-After. Internal session API only. |
| Security / High | Test origin may accidentally become deployment trust | Accepted with modified remedy. Phase1/3 test Env explicitly trusts its matching isolated origin; separate mismatched configured-origin/request-host case and loopback E2E prove boundaries. No test origin in deployment. This preserves Better Auth URL/cookie consistency without weakening the requested negative test. |
| Security / Medium | Auto-added Order contract header masks negative requests | Accepted. Contracts/Phase3 require raw missing/stale/malformed/valid header matrix with auth, membership, Origin and concealed-target precedence. |
| Failure / High | Missing Origin allegedly allowed | Policy-bypass allegation rejected; wording clarified. The allowance was for absent `Sec-Fetch-Site`, not absent Origin. Canonical text and Phase3 now explicitly require exact Origin even for test clients; both headers absent fails. No original security policy reversed. |
| Failure / High | Raw Wrangler proof can hit another local DB | Accepted. Phase8 requires proxy/raw-CLI sentinel and protected graph cross-reads, normalized persistence paths, actual migration entries and FK check from the same state. Test-pool memory evidence remains separate. |
| Failure / Medium | Local docs can overclaim remote authentication | Accepted. Phase8 distinguishes checked-out behavior from deployed state; deployed anonymous/unknown warning stays until separately authorized authenticated smoke. |
| Failure / Low | Aggregate review link absent during review | Transient draft-state finding, resolved here. The index now links this completed aggregate; not a runtime or architectural blocker. |
| Assumptions / High | Historical no-login AGENTS rule creates an implementation cycle | Hard-blocker interpretation rejected. Later explicit user approval of S4 implementation supersedes historical no-login scope for that change. Phase8 states this boundary; current planning does not authorize implementation or remote changes and does not prematurely rewrite product docs. |
| Assumptions / High | Operator proxy omits auth environment inputs | Accepted. Phase8 constructs typed DB/FILES/origin/runtime-secret inputs explicitly, handles named-environment non-inheritance and validates before auth creation; dry-run must have zero proxy/network/write calls. |
| Assumptions / High | Raw migration proof has no executable command | Accepted. Phase8 names `verification:s4-rehearsal`, its script, validated isolated paths, baseline schema7 checkout, actual migrations-apply bookkeeping and schema9-to10 coverage. No invented Wrangler through flag or applied SQL rewrite. |
| Assumptions / Medium | Membership assumption could become schema without approval | Resolved by the user's 2026-09-12 answer: separate accounts per Store, at most one active membership each, no picker. Propagated to Phase2/3/7/8 and contracts. This resolves the product decision only; implementation authorization and Phase1 runtime proof remain required. |
| Scope / High | Storefront E2E hides Console setup consumers | Accepted. Phase7 explicitly owns Owner-authenticated Product creation/edit and direct Order mutation helpers inside `storefront-orders.spec.ts`, exact Origin and separate public/capability clients. |
| Scope / High | Assignment result lacks shared command envelope | Accepted. Contracts and Phases2/5/7 specify `assign` result, `assigned` event, immutable assignee/event/status/time, migration fields and existing result-history ledger reader/recovery. Non-assignment/Customer response shapes stay unchanged. |
| Scope / Medium | E2E/rehearsal local proxy ownership duplicated | Accepted. Phase7 owns schema-independent `local-binding-context.ts` and one persistence-root contract; proxy-to-browser sentinel gates E2E. Phase8 reuses it and adds raw-CLI graph evidence, not a second local transport. |

## Additional controller consistency corrections

- Phase6 terminal actor/time fields link the winning decision via existing history `refund_request_id`; removed wording suggesting a second cyclic Refund-to-history FK/event column.
- Phase8 separates schema7 populated data from intermediate schema9 assignment fixtures; it cannot seed future tables into the old schema.
- Named remote-smoke command now has an explicit proposed script owner and required package registration, rather than an optional unnamed implementation.
- Scenario S4-18 remains Critical in the canonical acceptance map and Phase6 matrix. Final severity reconciliation is recorded in validation.

## Verdict and evidence limits

Follow-up audit approved and applied on 2026-09-12: [4 additional corrections](./audit-260912-1301-nexus-s4-identity-store-isolation.md). This earlier review remains a historical snapshot; current phase ownership distinguishes Phase 2 assignment SQL from Phase 5 result/reader/Console contracts, and Phase 8 Node orchestration from workerd acceptance. The follow-up also closes CSV uncertain-commit retention and internal session-cookie forwarding gaps not covered by this review.

All technical findings are addressed or explicitly rejected with source/contract reasoning; no finding authorizes cutting the three originally confirmed product decisions. The user additionally confirmed separate accounts per Store on 2026-09-12, resolving the Phase2 membership decision. The plan is not automatically approved for implementation. Runtime, behavioral TDD, migration, browser and deployment gates remain NOT RUN.

Original reviews: [Security](./red-team-security-260912-0224.md), [Failure modes](./red-team-failure-260912-0224.md), [Assumptions](./red-team-assumptions-260912-0224.md), [Scope/contracts](./red-team-scope-260912-0224.md). Current contracts: [canonical specification](../contracts.md), [validation record](./validation-260912-0224.md).
